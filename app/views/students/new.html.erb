<%= form_for @student, :url => students_save_path, :html => { :multipart => true } do |f| %>
  <% if @student.errors.any? %>
    <div class="error_explanation">
      <span><b>Errors</b></span>
      <ul>
        <% @student.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class = "new_form">
    <div>
      <%= f.label :name %><br>
      <%= f.text_field :name, :class => "required form-control" %>
    </div>

    <div>
      <%= f.label :email %><br>
      <%= f.email_field :email, autofocus: true, :class => "required form-control" %>
    </div>

    <div>
      <%= f.label :phone_no %><br>
      <%= f.text_field :phone_no, :class => "required form-control" %>
    </div>

    <div>
      <%= f.label :password %> <br>
      <%= f.password_field :password, autocomplete: "off", :class => "required form-control" %>
    </div>

    <div>
      <%= f.label :password_confirmation %><br>
      <%= f.password_field :password_confirmation, autocomplete: "off",:class => "required form-control" %>
    </div><br>

    <div>
      <%= f.file_field :avatar %>
      <img id="img_prev" src="#" alt="" />
    </div><br>

    <div id = "image_div" style = "display:none">
      <%= f.file_field :avatar1 %>
      <img id="img_prev2" src="#" alt="" />
    </div><br>

    <div><%= f.submit class: 'btn btn-primary' %></div>
  </div>
<% end %>